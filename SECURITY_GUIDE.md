# IP Checker Pro - Руководство по безопасности

## 🔐 Критические меры безопасности

### 1. SECRET_KEY

**ОБЯЗАТЕЛЬНО** установите уникальный SECRET_KEY перед запуском в production:

```bash
# Generate a secure key
python -c "import secrets; print(secrets.token_hex(32))"

# Set as environment variable
export SECRET_KEY="your-generated-key-here"
```

### 2. Локальный доступ

По умолчанию приложение работает в режиме `LOCAL_ONLY=True`, что разрешает доступ только с:
- 127.0.0.0/8 (localhost)
- 10.0.0.0/8 (private)
- 172.16.0.0/12 (private)
- 192.168.0.0/16 (private)
- IPv6 loopback и link-local адреса

**Никогда** не отключайте LOCAL_ONLY без настройки аутентификации!

### 3. Rate Limiting

Приложение использует Flask-Limiter для защиты от:
- DDoS атак
- Исчерпания лимитов внешних API
- Перегрузки сервера

Лимиты по умолчанию:
- 200 запросов в день
- 50 запросов в час
- 30 запросов в минуту для тяжелых endpoints

### 4. Security Headers

Flask-Talisman добавляет следующие заголовки:
- Content-Security-Policy
- X-Content-Type-Options
- X-Frame-Options
- Strict-Transport-Security (в production)

### 5. Валидация входных данных

Все IP-адреса валидируются перед обработкой:
- Проверка формата IPv4/IPv6
- Отклонение невалидных адресов
- Защита от SSRF

## 🚀 Запуск в production

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements_improved.txt
```

### Шаг 2: Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env файл
nano .env
```

### Шаг 3: Настройка WSGI сервера

**НЕ используйте встроенный сервер Flask в production!**

Используйте Gunicorn:

```bash
pip install gunicorn
gunicorn -w 4 -b 127.0.0.1:5000 app_improved:app
```

### Шаг 4: Настройка Nginx (рекомендуется)

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### Шаг 5: HTTPS

**ВСЕГДА** используйте HTTPS в production:

```bash
# Set in .env
FORCE_HTTPS=True
```

## 🔍 Проверка безопасности

### Bandit - Security linter

```bash
pip install bandit
bandit -r app_improved.py
```

### Safety - Dependency vulnerability scanner

```bash
pip install safety
safety check
```

### Pip-audit

```bash
pip install pip-audit
pip-audit -r requirements_improved.txt
```

## ⚠️ Известные ограничения

1. **Нет аутентификации** - приложение не имеет системы логина/пароля
2. **Нет авторизации** - нет разграничения прав доступа
3. **Локальное хранение** - кэш хранится в памяти (теряется при перезапуске)
4. **Нет аудита** - действия пользователей не логируются

## 🛡️ Рекомендации

### Для локального использования
- Оставьте `LOCAL_ONLY=True`
- Используйте firewall для блокировки внешнего доступа
- Регулярно обновляйте зависимости

### Для production
1. Добавьте аутентификацию (Flask-Login)
2. Настройте логирование действий
3. Используйте Redis для кэширования
4. Настройте мониторинг (Prometheus/Grafana)
5. Регулярно сканируйте на уязвимости

## 📞 Контакты для security issues

При обнаружении уязвимостей:
1. Не создавайте public issue
2. Отправьте описание на security@your-domain.com
3. Дождитесь исправления перед публикацией

---

*Последнее обновление: 2026-02-06*
